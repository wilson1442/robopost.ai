# robopost.ai Environment Variables
# Copy this file to .env.local and fill in the values

# ============================================
# Phase 1: Early Access (Required)
# ============================================

# Webhook URL for early access form submissions
# This is the n8n webhook endpoint that receives early access requests
EARLY_ACCESS_WEBHOOK_URL=https://your-n8n-instance.com/webhook/early-access

# ============================================
# Phase 2+: Supabase Configuration
# ============================================

# Supabase project URL (public, safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public key (public, safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (private, server-side only)
# DO NOT expose this in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# Phase 2+: n8n Configuration
# ============================================

# Base URL for n8n webhook endpoints
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook

# Secret key for x-n8n-signature header authentication (required)
# Used in the x-n8n-signature header for outbound webhook requests to n8n
N8N_WEBHOOK_SECRET=your-webhook-secret-here
